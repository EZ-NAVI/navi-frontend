<resources>

    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>

        <!-- Material Components colors -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
    </style>

    <!-- ⭐ 스플래시 테마 추가 -->
    <style name="BootSplashTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/bootsplash</item>
        <!-- Make status bar white on splash to match other screens -->
        <item name="android:statusBarColor">@color/white</item>
        <!-- Use dark status bar icons/text on white background -->
        <item name="android:windowLightStatusBar">true</item>
    </style>

</resources>
